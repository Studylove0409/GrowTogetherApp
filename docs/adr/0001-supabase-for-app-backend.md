# Supabase for the app backend

The product is being developed as a Flutter app, so the backend will serve the app directly rather than follow the older WeChat mini-program cloud-development direction in the PRD. We chose Supabase because the core model is relational: users, couple relationships, plans, checkins, and reminders need clear ownership and row-level access rules between two bound partners.
